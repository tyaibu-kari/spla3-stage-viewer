"use strict";
/* =========================================================
   rangescale.js - 射程スケールの初期値(あらかじめ設定しておく値)
   -----------------------------------------------------------
   通常、射程スケールは各ユーザーがブラウザで「📏 射程スケールを設定」を
   使って1回ずつ校正し、その結果はブラウザのlocalStorageにだけ保存されます
   (=他の人には配られない/公開版にも入らない)。

   このファイルに値を入れておくと、まだ校正していない人がステージを開いた
   ときの「初期値」として自動的に使われるようになります(app.js側で対応)。
   ユーザー自身がそのステージで校正し直すと、その人のブラウザ内では
   その校正値が優先されます(このファイルの値は上書きされません)。

   ■ キーの形式
     "モード::ステージ名(base)" 単位で個別に指定できます。
     例: "ナワバリ::ユノハナ大渓谷": { "pxPerUnit": 42.3 }

     また、モードをまたいで同じ値を使いたい場合は "*::ステージ名(base)" の
     形式で指定すると、そのステージ名を持つ全モードの初期値になります
     (個別の "モード::ステージ名" が指定されている場合はそちらが優先されます)。
     例: "*::ユノハナ大渓谷": { "pxPerUnit": 42.3 }

   ■ 値の決め方・埋め方
     Claudeに「◯◯を基準に射程スケールを一括で決めて、このファイルを更新して」
     と頼めば、以下のいずれかの方法で自動生成できます。
       (a) 全ステージ画像が同じ倍率で書き出されている場合 → 一つの
           pxPerUnit(またはdpi等の元情報)を教えてもらえれば、全件に同じ値を設定
       (b) ステージごとに倍率が違う場合 → 各ステージを一度アプリ上で
           校正 → ヘッダーの「⤓ データ書き出し」で書き出したJSONを渡して
           もらえれば、そこからこのファイルを自動生成
   ========================================================= */
const DEFAULT_RANGE_SCALE = {
  // 例: "*::ユノハナ大渓谷": { "pxPerUnit": 42.3 },
};
